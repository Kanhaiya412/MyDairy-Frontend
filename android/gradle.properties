# --- Performance Boost (Fixes the 19-minute wait) ---
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.configureondemand=true

# --- Architecture Fix (Fixes the "Failed to Install" error) ---
# This tells Gradle to build for your Samsung (arm64) AND the emulator (x86_64)
reactNativeArchitectures=arm64-v8a,x86_64

# --- Required Settings ---
android.useAndroidX=true
newArchEnabled=true
hermesEnabled=true
edgeToEdgeEnabled=false

# --- Path to Java ---
org.gradle.java.home=C:\\Users\\karti\\.jdks\\corretto-17.0.17